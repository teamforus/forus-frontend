doctype
html(lang="nl" ng-controller="BaseController" ng-cloak)
    include ../layout/includes/head
    body(modal-scroll-braker printable-enabler)
        page-loading-bar
        ui-view
        div(forus-chat chat-id="appConfigs.chat_id" ng-if="appConfigs.chat_id && $root.auth_user")
        include ../layout/includes/footer
        
        printables-root
        push-notifications
        
        include ../layout/includes/scripts

if (qdt_c.git_log_header) 
    | <!-- !{qdt_c.git_log_header} -->