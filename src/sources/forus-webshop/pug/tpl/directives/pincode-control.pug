.block.block-pincode
    input.hidden-input(ng-model="ngModel")

    .pincode-number(
        ng-repeat="code in chars track by $index" 
        ng-class="{'pincode-number-divider' : code=='split'}"
        ng-bind="code != 'split' ? code : ''" )