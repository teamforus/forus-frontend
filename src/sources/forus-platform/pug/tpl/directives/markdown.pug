.block.block-markdownable(ng-class="(allowAlignment ? 'block-markdownable-' + (blockAlignment || 'left') : '')")
    textarea.markdown-editor.block.block-markdown(editor name="test" id="{{ editorId }}")