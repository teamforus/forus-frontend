.app.app-container
    menu
    section.app.app-content
        //- Breadcrumbs
        .block.block-breadcrumbs
            .breadcrumb-item(ui-sref="reimbursements({organization_id: $ctrl.organization.id})") Reimbursements
            .breadcrumb-item.active Reimbursement categories

        reimbursement-categories(organization="$ctrl.organization")