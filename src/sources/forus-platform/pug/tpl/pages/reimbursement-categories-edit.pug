.app.app-container
    menu
    section.app.app-content
        //- Breadcrumbs
        .block.block-breadcrumbs
            .breadcrumb-item(ui-sref="reimbursements({organization_id: $ctrl.organization.id})") Declaraties
            .breadcrumb-item.active Declaratie categorieën

        reimbursement-categories(organization="$ctrl.organization")