.wrapper
    .text-center
        button(ng-click="$ctrl.requestAuthToken()").button.button-primary Sign In
    div(ng-show="$ctrl.showModal").modal
        .modal-backdrop(ng-click="$ctrl.closeModal()")
        .modal-window
            .modal-header Sign In
            .modal-body.form
                .modal-section
                    .qrcode: #qrcode
            .modal-footer.text-center
                button(ng-click="$ctrl.closeModal()").button.button-primary Cancel