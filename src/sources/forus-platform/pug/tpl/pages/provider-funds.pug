.app.app-container
    menu
    section.app.app-content
        .card-heading Funds ({{ $ctrl.funds.length }}) 
        div(
            ng-if="$ctrl.funds.length > 0" 
            ng-repeat="fund in $ctrl.funds" 
            ng-show="fund.state != 'pending'"
            fund-card-provider 
            provider-fund="fund" 
            organization="$ctrl.organization")
        empty-block(
            ng-if="$ctrl.funds.length == 0" 
            text="'Sorry, you have no active funds.'"
            button="{text: 'Check available funds', 'href': $ctrl.emptyBlockLink}")