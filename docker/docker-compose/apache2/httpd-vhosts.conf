<VirtualHost *:80>
    ServerName webshop.forus.com

    DocumentRoot /usr/local/apache2/htdocs/webshop
    ServerSignature On
    Options FollowSymLinks Includes MultiViews ExecCGI
    UseCanonicalName Off

    RewriteEngine On

    # If an existing asset or directory is requested go to it as it is
    RewriteCond %{DOCUMENT_ROOT}%{REQUEST_URI} -f [OR]
    RewriteCond %{DOCUMENT_ROOT}%{REQUEST_URI} -d
    RewriteRule ^ - [L]

    # If the requested resource doesn't exist, use index.html
    RewriteRule ^ /index.html
</VirtualHost>

<VirtualHost *:3000>
    ServerName provider.forus.com

    DocumentRoot /usr/local/apache2/htdocs/provider
    ServerSignature On
    Options FollowSymLinks Includes MultiViews ExecCGI
    UseCanonicalName Off

    RewriteEngine On

    # If an existing asset or directory is requested go to it as it is
    RewriteCond %{DOCUMENT_ROOT}%{REQUEST_URI} -f [OR]
    RewriteCond %{DOCUMENT_ROOT}%{REQUEST_URI} -d
    RewriteRule ^ - [L]

    # If the requested resource doesn't exist, use index.html
    RewriteRule ^ /index.html
</VirtualHost>

<VirtualHost *:4000>
    ServerName sponsor.forus.com

    DocumentRoot /usr/local/apache2/htdocs/sponsor
    ServerSignature On
    Options FollowSymLinks Includes MultiViews ExecCGI
    UseCanonicalName Off

    RewriteEngine On

    # If an existing asset or directory is requested go to it as it is
    RewriteCond %{DOCUMENT_ROOT}%{REQUEST_URI} -f [OR]
    RewriteCond %{DOCUMENT_ROOT}%{REQUEST_URI} -d
    RewriteRule ^ - [L]

    # If the requested resource doesn't exist, use index.html
    RewriteRule ^ /index.html
</VirtualHost>

<VirtualHost *:5000>
    ServerName validator.forus.com

    DocumentRoot /usr/local/apache2/htdocs/validator
    ServerSignature On
    Options FollowSymLinks Includes MultiViews ExecCGI
    UseCanonicalName Off

    RewriteEngine On

    # If an existing asset or directory is requested go to it as it is
    RewriteCond %{DOCUMENT_ROOT}%{REQUEST_URI} -f [OR]
    RewriteCond %{DOCUMENT_ROOT}%{REQUEST_URI} -d
    RewriteRule ^ - [L]

    # If the requested resource doesn't exist, use index.html
    RewriteRule ^ /index.html
</VirtualHost>